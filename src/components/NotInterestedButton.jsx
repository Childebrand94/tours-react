const NotInterestedButton = ({ onClick }) => {
  return (
    <button onClick={onClick} className="delete-btn btn-block btn">
      Not Interested
    </button>
  )
}
export default NotInterestedButton
